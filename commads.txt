list global details

>git config --list

adding username

>git config --global user.name "username"

adding user email

>git config --global user.email "useremail"

command for  initializing git in pwd

>git init

command for adding all untracked files to staging area

>git add -A

command for adding staged files to localrepository

>git commit -m "commit message"

listing all untracked files

>git status


Exact copy of file from local repository to local workspace

>gti clone 


>git push [local repository to remote repository]

git pull [ change from  remote repository to local workspace]



git config --list[to list configuration]

git config --global user.name "user name"[to get username]


git config --global user.email "user email"[to get user email]


git remote add remote name remote path